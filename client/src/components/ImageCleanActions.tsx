/** Audit Ledger style: all clean-copy evidence, previews, and reports remain inside the active browser tab. */
import { KeyboardEvent, PointerEvent, useEffect, useMemo, useState } from "react";
import { AlertTriangle, Check, Copy, Download, Eraser, Eye, FileJson, Palette, RotateCcw, Search, ShieldCheck, SlidersHorizontal } from "lucide-react";
import { Button } from "@/components/ui/button";
import { canCreateCleanCopy, cleanCopyFileExtension, CleanCopyFormat, cleanCopyFileName, createExifFreeImage, DEFAULT_JPEG_QUALITY, ImageInspection, JPEG_QUALITY_MAX, JPEG_QUALITY_MIN, needsOrientationCorrection, outputDimensionsForResize, RESIZE_LONG_EDGE_OPTIONS, ResizeOptions } from "@/lib/image";
import { MetadataCsvReport } from "@/components/MetadataCsvReport";

type CleanStage = "idle" | "working" | "complete" | "error";
type SimpleStage = "idle" | "complete" | "error";
type PreviewKind = "original" | "clean";
type JpegMode = "baseline-compatible";

const PREFERENCE_KEY = "private-preflight:image-clean-copy-preferences:v3";
const formatBytes = (bytes: number) => bytes < 1024 ? `${bytes} B` : bytes < 1024 * 1024 ? `${(bytes / 1024).toFixed(1)} KB` : `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
const sizeCategory = (bytes: number | null) => bytes === null ? "not-estimated" : bytes < 1_024 ? "under-1KB" : bytes < 100_000 ? "under-100KB" : bytes < 1_000_000 ? "under-1MB" : "1MB-or-more";
const metadataValue = (value?: string) => value || "Not recorded";
const orientationLabel = (value?: number) => !value ? "Not recorded" : ({ 1: "Normal", 3: "Rotated 180°", 6: "Rotated 90° clockwise", 8: "Rotated 90° counter-clockwise" }[value] ?? `EXIF orientation ${value}`);
const clampDimension = (value: number, maximum: number) => Math.min(maximum, Math.max(1, Math.round(value) || 1));

function readPreferences(): { quality: number; advancedOpen: boolean; jpegMode: JpegMode } {
  try {
    const value = JSON.parse(localStorage.getItem(PREFERENCE_KEY) ?? "{}");
    return { quality: Number.isFinite(value.quality) ? Math.min(JPEG_QUALITY_MAX, Math.max(JPEG_QUALITY_MIN, Math.round(value.quality))) : DEFAULT_JPEG_QUALITY, advancedOpen: Boolean(value.advancedOpen), jpegMode: "baseline-compatible" };
  } catch { return { quality: DEFAULT_JPEG_QUALITY, advancedOpen: false, jpegMode: "baseline-compatible" }; }
}

function downloadBlob(blob: Blob, fileName: string) {
  const url = URL.createObjectURL(blob);
  const link = document.createElement("a");
  link.href = url;
  link.download = fileName;
  link.click();
  window.setTimeout(() => URL.revokeObjectURL(url), 0);
}

async function copySummary(text: string) {
  if (navigator.clipboard?.writeText) { await navigator.clipboard.writeText(text); return; }
  const area = document.createElement("textarea");
  area.value = text;
  area.style.position = "fixed";
  area.style.opacity = "0";
  document.body.appendChild(area);
  area.select();
  const copied = document.execCommand("copy");
  area.remove();
  if (!copied) throw new Error("Clipboard access was unavailable. Select the summary and copy it manually.");
}

function Magnifier({ sourceUrl, cleanUrl, quality, format }: { sourceUrl: string; cleanUrl: string; quality: number; format: Exclude<CleanCopyFormat, "png"> }) {
  const [active, setActive] = useState<PreviewKind>("clean");
  const [zoom, setZoom] = useState(3);
  const [point, setPoint] = useState({ x: 50, y: 50 });
  const activeUrl = active === "original" ? sourceUrl : cleanUrl;
  const updatePoint = (event: PointerEvent<HTMLDivElement>) => {
    const rect = event.currentTarget.getBoundingClientRect();
    setPoint({ x: Math.min(90, Math.max(10, ((event.clientX - rect.left) / rect.width) * 100)), y: Math.min(90, Math.max(10, ((event.clientY - rect.top) / rect.height) * 100)) });
  };
  const moveWithKeyboard = (event: KeyboardEvent<HTMLDivElement>) => {
    if (!( ["ArrowLeft", "ArrowRight", "ArrowUp", "ArrowDown"] as string[]).includes(event.key)) return;
    event.preventDefault();
    setPoint(({ x, y }) => ({ x: event.key === "ArrowLeft" ? Math.max(10, x - 5) : event.key === "ArrowRight" ? Math.min(90, x + 5) : x, y: event.key === "ArrowUp" ? Math.max(10, y - 5) : event.key === "ArrowDown" ? Math.min(90, y + 5) : y }));
  };
  const formatName = format.toUpperCase();
  return <section className="jpeg-magnifier" aria-labelledby="magnifier-title"><div className="jpeg-magnifier__head"><span className="eyebrow"><span>ZOOM / 05</span> Detail lens</span><h4 id="magnifier-title">Inspect local compression details.</h4></div><div className="magnifier-picker" role="group" aria-label="Preview to magnify"><button type="button" className={active === "original" ? "is-selected" : ""} aria-pressed={active === "original"} onClick={() => setActive("original")}>Original</button><button type="button" className={active === "clean" ? "is-selected" : ""} aria-pressed={active === "clean"} onClick={() => setActive("clean")}>Clean {formatName} · {quality}</button></div><div className="magnifier-stage" tabIndex={0} role="application" aria-label="Move pointer or arrow keys to inspect a magnified local preview" onPointerMove={updatePoint} onKeyDown={moveWithKeyboard}><img src={activeUrl} alt={active === "original" ? "Original image detail" : `Clean ${formatName} detail at quality ${quality}`} /><span className="magnifier-lens" style={{ left: `${point.x}%`, top: `${point.y}%`, backgroundImage: `url(${activeUrl})`, backgroundPosition: `${point.x}% ${point.y}%`, backgroundSize: `${zoom * 100}%` }} aria-hidden="true" /></div><label className="magnifier-zoom"><Search aria-hidden="true" /> Lens zoom <input type="range" min="2" max="5" step="0.5" value={zoom} onChange={(event) => setZoom(Number(event.target.value))} /> <strong>{zoom}×</strong></label><p>Move the pointer or use arrow keys. This lens uses the same local preview as the selected download setting.</p></section>;
}

export function ImageCleanActions({ sourceFile, inspection }: { sourceFile: File | null; inspection: ImageInspection }) {
  const [initialPreferences] = useState(readPreferences);
  const [cleanFormat, setCleanFormat] = useState<CleanCopyFormat>("jpeg");
  const [quality, setQuality] = useState(initialPreferences.quality);
  const [jpegMode] = useState<JpegMode>(initialPreferences.jpegMode);
  const [advancedOpen, setAdvancedOpen] = useState(initialPreferences.advancedOpen);
  const [resizeEnabled, setResizeEnabled] = useState(false);
  const [resizeWidth, setResizeWidth] = useState(inspection.width);
  const [resizeHeight, setResizeHeight] = useState(inspection.height);
  const [resizeExact, setResizeExact] = useState(false);
  const [anonymizeOutputName, setAnonymizeOutputName] = useState(true);
  const [estimatedBytes, setEstimatedBytes] = useState<number | null>(null);
  const [estimating, setEstimating] = useState(false);
  const [sourcePreviewUrl, setSourcePreviewUrl] = useState<string | null>(null);
  const [cleanPreviewUrl, setCleanPreviewUrl] = useState<string | null>(null);
  const [cleanStage, setCleanStage] = useState<CleanStage>("idle");
  const [cleanError, setCleanError] = useState<string | null>(null);
  const [copyStage, setCopyStage] = useState<SimpleStage>("idle");
  const [copyError, setCopyError] = useState<string | null>(null);
  const [reportStage, setReportStage] = useState<SimpleStage>("idle");
  const [reportError, setReportError] = useState<string | null>(null);
  const canDownload = Boolean(sourceFile && canCreateCleanCopy(inspection.format, inspection.metadataState, inspection.ancillaryMetadata));
  const resizeOptions = useMemo<ResizeOptions | null>(() => resizeEnabled ? { maxWidth: resizeWidth, maxHeight: resizeHeight, exact: resizeExact } : null, [resizeEnabled, resizeExact, resizeHeight, resizeWidth]);
  const outputDimensions = useMemo(() => outputDimensionsForResize(inspection.width, inspection.height, resizeOptions), [inspection.height, inspection.width, resizeOptions]);
  const orientationCorrected = needsOrientationCorrection(inspection.exif.orientation);
  const outputFileName = sourceFile ? cleanCopyFileName(sourceFile.name, cleanFormat, anonymizeOutputName) : `private-preflight-image-clean.${cleanCopyFileExtension(cleanFormat)}`;
  const isLossy = cleanFormat === "jpeg" || cleanFormat === "webp";
  const formatName = cleanFormat.toUpperCase();
  const estimatedChange = sourceFile && estimatedBytes !== null ? estimatedBytes - sourceFile.size : null;

  useEffect(() => { try { localStorage.setItem(PREFERENCE_KEY, JSON.stringify({ quality, advancedOpen, jpegMode })); } catch { /* Optional local settings never block the tool. */ } }, [quality, advancedOpen, jpegMode]);
  useEffect(() => { setResizeEnabled(false); setResizeExact(false); setResizeWidth(inspection.width); setResizeHeight(inspection.height); }, [inspection.height, inspection.width, sourceFile]);
  useEffect(() => { if (!sourceFile) { setSourcePreviewUrl(null); return; } const url = URL.createObjectURL(sourceFile); setSourcePreviewUrl(url); return () => URL.revokeObjectURL(url); }, [sourceFile]);
  useEffect(() => { if (!sourceFile || !canDownload) return; let active = true; let previewUrl: string | null = null; setEstimating(true); setEstimatedBytes(null); const timer = window.setTimeout(() => { void createExifFreeImage(sourceFile, cleanFormat, quality, resizeOptions).then((blob) => { if (!active) return; setEstimatedBytes(blob.size); if (cleanFormat !== "png") { previewUrl = URL.createObjectURL(blob); setCleanPreviewUrl(previewUrl); } else setCleanPreviewUrl(null); }).catch(() => { if (active) { setEstimatedBytes(null); setCleanPreviewUrl(null); } }).finally(() => { if (active) setEstimating(false); }); }, 180); return () => { active = false; window.clearTimeout(timer); if (previewUrl) URL.revokeObjectURL(previewUrl); }; }, [sourceFile, cleanFormat, quality, resizeOptions, canDownload]);

  const privacySummary = ["Private Preflight image privacy summary", "Inspection ran locally in this browser tab.", `EXIF metadata: ${inspection.metadataState === "available" ? "detected" : "not detected"}.`, `Location metadata: ${inspection.exif.hasLocationMetadata ? "detected" : "not detected"}.`, `ICC profile: ${inspection.ancillaryMetadata.hasIccProfile ? "detected and removed in the clean output" : "not detected"}.`, `Text comments: ${inspection.ancillaryMetadata.hasTextComments ? "detected and removed in the clean output" : "not detected"}.`, `XMP metadata: ${inspection.ancillaryMetadata.hasXmp ? "detected and removed in the clean output" : "not detected"}.`, orientationCorrected ? "EXIF orientation: corrected into clean pixels before metadata removal." : "EXIF orientation: no correction needed.", canDownload ? `Selected clean output: ${formatName}${isLossy ? ` at quality ${quality}` : ""}; dimensions ${outputDimensions.width} × ${outputDimensions.height}px${outputDimensions.resized ? " after local resizing" : " (original dimensions)"}; ${anonymizeOutputName ? "anonymous output filename" : "source-derived output filename"}.` : "No clean copy is offered for this image.", "The original image is not changed or uploaded. This summary excludes filename, image bytes, camera values, and coordinates."].join("\n");
  const report = useMemo(() => ({ schemaVersion: "1.3", generatedAt: new Date().toISOString(), processing: { location: "local browser tab", uploaded: false, originalModified: false }, source: { format: inspection.format, mimeType: inspection.mimeType, dimensions: { width: inspection.width, height: inspection.height }, megapixels: inspection.megapixels, fileSizeCategory: sizeCategory(inspection.fileSize) }, metadataSignals: { exif: inspection.metadataState, location: inspection.exif.hasLocationMetadata ? "detected" : "not-detected", iccProfile: inspection.ancillaryMetadata.hasIccProfile ? "detected" : "not-detected", textComments: inspection.ancillaryMetadata.hasTextComments ? "detected" : "not-detected", xmp: inspection.ancillaryMetadata.hasXmp ? "detected" : "not-detected" }, cleanCopy: { offered: canDownload, selectedFormat: cleanFormat, jpegQuality: cleanFormat === "jpeg" ? quality : null, webpQuality: cleanFormat === "webp" ? quality : null, jpegMode: cleanFormat === "jpeg" ? "baseline-compatible-browser-output" : null, resizeOptions, outputDimensions, outputName: anonymizeOutputName ? "anonymous" : "derived-from-source-locally", orientationCorrection: orientationCorrected ? "normalized-locally" : "not-needed", estimatedSizeCategory: sizeCategory(estimatedBytes) }, privacyBoundary: { excludes: ["source filename", "image bytes", "preview URLs", "raw EXIF values", "XMP contents", "camera make or model", "capture time", "coordinates", "user identifiers"] } }), [anonymizeOutputName, canDownload, cleanFormat, estimatedBytes, inspection, orientationCorrected, outputDimensions, quality, resizeOptions]);
  const download = async () => { if (!sourceFile) return; setCleanStage("working"); setCleanError(null); try { downloadBlob(await createExifFreeImage(sourceFile, cleanFormat, quality, resizeOptions), outputFileName); setCleanStage("complete"); } catch (error) { setCleanError(error instanceof Error ? error.message : "The browser could not create the clean copy locally."); setCleanStage("error"); } };
  const copy = async () => { setCopyStage("idle"); setCopyError(null); try { await copySummary(privacySummary); setCopyStage("complete"); } catch (error) { setCopyError(error instanceof Error ? error.message : "Clipboard access was unavailable."); setCopyStage("error"); } };
  const downloadReport = () => { setReportStage("idle"); setReportError(null); try { downloadBlob(new Blob([JSON.stringify(report, null, 2)], { type: "application/json" }), "private-preflight-image-privacy-report.json"); setReportStage("complete"); } catch (error) { setReportError(error instanceof Error ? error.message : "The browser could not download the local JSON report."); setReportStage("error"); } };
  const resetPreferences = () => { try { localStorage.removeItem(PREFERENCE_KEY); } catch { /* Ignore unavailable storage. */ } setQuality(DEFAULT_JPEG_QUALITY); setAdvancedOpen(false); };
  const applyLongEdgePreset = (longEdge: number) => { const cap = Math.min(longEdge, Math.max(inspection.width, inspection.height)); setResizeWidth(Math.min(inspection.width, cap)); setResizeHeight(Math.min(inspection.height, cap)); setResizeExact(false); setCleanStage("idle"); };
  const chooseFormat = (format: CleanCopyFormat) => { setCleanFormat(format); setCleanStage("idle"); };

  return <>
    {canDownload && <><section className="metadata-comparison" aria-labelledby="comparison-title"><div className="metadata-comparison__heading"><span className="eyebrow"><span>COMPARE / 04</span> Metadata ledger</span><h3 id="comparison-title">See what the clean copy removes.</h3></div><div className="metadata-comparison__table"><div className="metadata-comparison__header"><span>Field</span><span>Original</span><span>Clean {formatName}</span></div><div><span>EXIF block</span><strong>{inspection.metadataState === "available" ? "Found" : "Present, unreadable"}</strong><strong className="metadata-removed">Removed</strong></div><div><span>Orientation</span><strong>{orientationLabel(inspection.exif.orientation)}</strong><strong className="metadata-removed">{orientationCorrected ? "Corrected + removed" : "Normalized"}</strong></div><div><span>Camera make</span><strong>{metadataValue(inspection.exif.make)}</strong><strong className="metadata-removed">Removed</strong></div><div><span>Camera model</span><strong>{metadataValue(inspection.exif.model)}</strong><strong className="metadata-removed">Removed</strong></div><div><span>Capture time</span><strong>{metadataValue(inspection.exif.capturedAt)}</strong><strong className="metadata-removed">Removed</strong></div><div><span>Location signal</span><strong>{inspection.exif.hasLocationMetadata ? "Present" : "Not recorded"}</strong><strong className="metadata-removed">{inspection.exif.hasLocationMetadata ? "Removed" : "Not recorded"}</strong></div><div><span>ICC colour profile</span><strong>{inspection.ancillaryMetadata.hasIccProfile ? "Detected" : "Not recorded"}</strong><strong className="metadata-removed">{inspection.ancillaryMetadata.hasIccProfile ? "Removed" : "Not recorded"}</strong></div><div><span>Text comments</span><strong>{inspection.ancillaryMetadata.hasTextComments ? "Detected" : "Not recorded"}</strong><strong className="metadata-removed">{inspection.ancillaryMetadata.hasTextComments ? "Removed" : "Not recorded"}</strong></div><div><span>XMP packet</span><strong>{inspection.ancillaryMetadata.hasXmp ? "Detected" : "Not recorded"}</strong><strong className="metadata-removed">{inspection.ancillaryMetadata.hasXmp ? "Removed" : "Not recorded"}</strong></div></div></section>
    <section className="image-clean-copy" aria-live="polite"><div><span className="eyebrow"><span>DECIDE / 05</span> Local re-encode</span><h3>Remove metadata and download a clean copy.</h3><p>Choose compact JPEG or WebP, or a lossless PNG. The original image is not changed.</p><div className="clean-format-picker" role="group" aria-label="Clean-copy format"><button type="button" aria-pressed={cleanFormat === "jpeg"} className={cleanFormat === "jpeg" ? "is-selected" : ""} onClick={() => chooseFormat("jpeg")}>JPEG <small>Compact · adjustable quality</small></button><button type="button" aria-pressed={cleanFormat === "webp"} className={cleanFormat === "webp" ? "is-selected" : ""} onClick={() => chooseFormat("webp")}>WebP <small>Compact · adjustable quality</small></button><button type="button" aria-pressed={cleanFormat === "png"} className={cleanFormat === "png" ? "is-selected" : ""} onClick={() => chooseFormat("png")}>PNG <small>Lossless · preserves transparency</small></button></div>{isLossy && <label className="jpeg-quality-control"><span><SlidersHorizontal aria-hidden="true" /> {formatName} quality <strong>{quality}</strong></span><input type="range" min={JPEG_QUALITY_MIN} max={JPEG_QUALITY_MAX} value={quality} onChange={(event) => { setQuality(Number(event.target.value)); setCleanStage("idle"); }} /><small>Lower quality generally means a smaller local file and more compression.</small></label>}{cleanFormat === "jpeg" && <label className="jpeg-mode-control"><span>JPEG scan mode</span><select value={jpegMode} aria-label="JPEG output scan mode"><option value="baseline-compatible">Baseline-compatible browser output</option></select><small>Progressive JPEG is not exposed by this browser encoder, so this local build does not offer a misleading switch.</small></label>}<section className="resize-control" aria-labelledby="resize-title"><div className="resize-control__heading"><label><input type="checkbox" checked={resizeEnabled} onChange={(event) => { setResizeEnabled(event.target.checked); setCleanStage("idle"); }} /> <span id="resize-title">Resize before sharing</span></label><small>Optional pixel resize; image data stays in this browser.</small></div>{resizeEnabled && <div className="resize-control__body"><div className="resize-control__inputs"><label>Max width <input type="number" inputMode="numeric" min={1} max={inspection.width} value={resizeWidth} onChange={(event) => { setResizeWidth(clampDimension(Number(event.target.value), inspection.width)); setCleanStage("idle"); }} /> <small>px · source {inspection.width}</small></label><label>Max height <input type="number" inputMode="numeric" min={1} max={inspection.height} value={resizeHeight} onChange={(event) => { setResizeHeight(clampDimension(Number(event.target.value), inspection.height)); setCleanStage("idle"); }} /> <small>px · source {inspection.height}</small></label></div><div className="resize-control__presets" role="group" aria-label="Resize longest-edge presets"><span>Quick limits</span>{RESIZE_LONG_EDGE_OPTIONS.map((value) => <button key={value} type="button" onClick={() => applyLongEdgePreset(value)}>{value}px</button>)}</div><label className="resize-control__exact"><input type="checkbox" checked={resizeExact} onChange={(event) => { setResizeExact(event.target.checked); setCleanStage("idle"); }} /> Use exact dimensions <small>May distort the image. Leave off to preserve proportions.</small></label><p className="resize-control__result">Output: <strong>{outputDimensions.width} × {outputDimensions.height}px</strong> · {outputDimensions.resized ? resizeExact ? "exact local dimensions" : "proportions preserved" : "original dimensions retained"}.</p></div>}</section>{sourcePreviewUrl && cleanPreviewUrl && isLossy && <><div className="jpeg-preview" aria-label={`Local ${formatName} quality preview`}><div><span>Original</span><img src={sourcePreviewUrl} alt="Original local image preview" /></div><div><span>Clean {formatName} · quality {quality}</span><img src={cleanPreviewUrl} alt={`Clean ${formatName} preview at quality ${quality}`} /></div><p><Eye aria-hidden="true" /> Local visual guide only. The downloaded copy uses this selected quality and dimensions.</p></div><Magnifier sourceUrl={sourcePreviewUrl} cleanUrl={cleanPreviewUrl} quality={quality} format={cleanFormat as Exclude<CleanCopyFormat, "png">} /></>}<details className="advanced-cleanup" open={advancedOpen} onToggle={(event) => setAdvancedOpen((event.currentTarget as HTMLDetailsElement).open)}><summary><Palette aria-hidden="true" /> Advanced container cleanup</summary><p>Colour profiles, text comments, and XMP packets can carry extra information. A clean browser re-encode removes each when found.</p><div><span>ICC profile: <strong>{inspection.ancillaryMetadata.hasIccProfile ? "Detected — will be removed" : "Not detected"}</strong></span><span>Text comments: <strong>{inspection.ancillaryMetadata.hasTextComments ? "Detected — will be removed" : "Not detected"}</strong></span><span>XMP: <strong>{inspection.ancillaryMetadata.hasXmp ? "Detected — will be removed" : "Not detected"}</strong></span></div><button type="button" className="reset-preferences" onClick={resetPreferences}><RotateCcw aria-hidden="true" /> Reset saved settings</button></details>{estimating ? <p className="clean-estimate">Creating local estimate and preview…</p> : estimatedBytes !== null ? <p className="clean-estimate">Estimated clean {formatName}: <strong>{formatBytes(estimatedBytes)}</strong>{isLossy ? ` at quality ${quality}` : ""}. {sourceFile && estimatedChange !== null ? <>Source {formatBytes(sourceFile.size)} → estimated output {estimatedChange === 0 ? "same size" : estimatedChange < 0 ? `${formatBytes(Math.abs(estimatedChange))} smaller` : `${formatBytes(estimatedChange)} larger`}.</> : null}</p> : <p className="clean-estimate">Size estimate becomes available before download.</p>}{cleanStage === "complete" && <p className="image-clean-copy__success"><ShieldCheck aria-hidden="true" /> Clean {formatName} downloaded locally. Review that new file before sharing.</p>}{cleanError && <p className="image-clean-copy__error"><AlertTriangle aria-hidden="true" /> {cleanError}</p>}</div><Button className="action-button" onClick={() => void download()} disabled={cleanStage === "working" || estimating}><Eraser aria-hidden="true" /> {cleanStage === "working" ? `Creating clean ${formatName}` : `Download clean ${formatName}`}<Download aria-hidden="true" /></Button></section></>}
    <section className="image-privacy-summary" aria-labelledby="privacy-summary-title"><div><span className="eyebrow"><span>SHARE / 06</span> Privacy summary</span><h3 id="privacy-summary-title">A concise local handling note.</h3><p>{privacySummary.split("\n").slice(1).join(" ")}</p>{copyStage === "complete" && <p className="image-privacy-summary__success"><Check aria-hidden="true" /> Privacy summary copied to clipboard.</p>}{copyError && <p className="image-privacy-summary__error"><AlertTriangle aria-hidden="true" /> {copyError}</p>}</div><div className="image-privacy-summary__actions"><label className="anonymous-output-control"><input type="checkbox" checked={anonymizeOutputName} onChange={(event) => { setAnonymizeOutputName(event.target.checked); setCleanStage("idle"); }} /> <span>Hide source filename</span><small>{outputFileName}</small></label><Button variant="outline" onClick={() => void copy()}><Copy aria-hidden="true" /> Copy privacy summary</Button></div></section>
    <section className="image-json-report" aria-labelledby="json-report-title"><div><span className="eyebrow"><span>RECORD / 07</span> Local evidence export</span><h3 id="json-report-title">Keep a privacy-safe JSON report.</h3><p>Includes metadata presence signals, clean-copy settings, and the local-only boundary. It excludes names, pixels, raw metadata values, and coordinates.</p>{reportStage === "complete" && <p className="image-json-report__success"><Check aria-hidden="true" /> Local JSON report downloaded.</p>}{reportError && <p className="image-json-report__error"><AlertTriangle aria-hidden="true" /> {reportError}</p>}</div><Button variant="outline" onClick={downloadReport}><FileJson aria-hidden="true" /> Download JSON report</Button></section>
    <MetadataCsvReport inspection={inspection} cleanFormat={cleanFormat} jpegQuality={quality} webpQuality={quality} resizeOptions={resizeOptions} estimatedBytes={estimatedBytes} cleanCopyOffered={canDownload} anonymizeOutputName={anonymizeOutputName} />
  </>;
}
